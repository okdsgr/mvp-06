## theme_config.gd
## ビジュアル定数の一元管理ファイル。
## デザインを変えたいときはここだけ編集すればOK。

class_name ThemeConfig

# ============================================================
# 背景
# ============================================================
const BG_COLOR := Color(0.08, 0.08, 0.12)          # 画面背景色

# ============================================================
# 大盤（グローバルグリッド）
# ============================================================
const GLOBAL_LINE_COLOR  := Color(0.9, 0.9, 0.9)   # 大盤の線色
const GLOBAL_LINE_WIDTH  := 6.0                      # 大盤の線幅(px)

# ============================================================
# 小盤（ローカルグリッド）
# ============================================================
const LOCAL_LINE_COLOR   := Color(0.5, 0.5, 0.55)   # 小盤の線色
const LOCAL_LINE_WIDTH   := 2.0                      # 小盤の線幅(px)

# ============================================================
# アクティブ小盤のハイライト
# ============================================================
const ACTIVE_BOARD_COLOR := Color(0.2, 0.6, 1.0, 0.18)  # 打てる盤の背景色
const INACTIVE_BOARD_COLOR := Color(0.0, 0.0, 0.0, 0.35) # 打てない盤の暗転色

# ============================================================
# ⭕（プレイヤー1）
# ============================================================
const P1_COLOR           := Color(0.2, 0.7, 1.0)    # ⭕の色
const P1_LINE_WIDTH      := 5.0                      # ⭕の線幅(px)
const P1_MARGIN          := 0.18                     # セルに対する余白比率（0〜0.5）

# ============================================================
# ❌（プレイヤー2）
# ============================================================
const P2_COLOR           := Color(1.0, 0.35, 0.35)  # ❌の色
const P2_LINE_WIDTH      := 5.0                      # ❌の線幅(px)
const P2_MARGIN          := 0.18                     # セルに対する余白比率（0〜0.5）

# ============================================================
# 小盤の勝者オーバーレイ（大きい⭕❌）
# ============================================================
const WINNER_P1_COLOR    := Color(0.2, 0.7, 1.0, 0.85)
const WINNER_P2_COLOR    := Color(1.0, 0.35, 0.35, 0.85)
const WINNER_DRAW_COLOR  := Color(0.5, 0.5, 0.5, 0.5)
const WINNER_LINE_WIDTH  := 12.0
const WINNER_MARGIN      := 0.08

# ============================================================
# UI（テキスト）
# ============================================================
const UI_FONT_COLOR      := Color(0.95, 0.95, 0.95)
const STATUS_FONT_SIZE   := 28
